1. git 推分支到另一个仓库
```
* git checkout -b branch-old
* git remote set-url origin 新的仓库地址
* git remote -v
* git push --set-upstream origin branch-old
* git clone new.git
* git checkout -b master
* git push 
```
